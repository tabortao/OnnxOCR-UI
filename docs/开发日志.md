## 20251204

feat(webui): 添加通过 URL 进行 OCR 识别的功能及优化文本处理

- 新增/ocr_url 接口支持通过图片 URL 进行 OCR 识别
- 添加 GET /r/{img_url:path}路径直连识别功能，返回纯文本
- 优化 OCR 结果文本处理，去除多余空格
- 更新文档说明新功能使用方法
- 修正平台支持描述并添加 ocr.html 前端页面

feat(webui): 添加通过 URL 进行 OCR 识别的功能

- 新增/ocr_url 接口支持通过图片 URL 进行 OCR 识别
- 添加 requests 和 aiofiles 依赖
- 更新 README 文档说明新接口使用方法
- 修正 README 中关于应用支持的平台描述

feat: 重构项目为 FastAPI 并添加 Docker 支持

- 将 Flask 应用迁移至 FastAPI 框架
- 新增 Dockerfile 和 docker-compose.yml 部署配置
- 添加 GitHub Actions CI/CD 工作流
- 升级 Python 版本至 3.12 并优化依赖管理
- 删除冗余文件 webui2.py 和 webui.html
- 更新开发日志和文档说明
